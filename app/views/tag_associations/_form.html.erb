<%= form_with(model: tag_association, local: true) do |form| %>
  <% if tag_association.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tag_association.errors.count, "error") %> prohibited this tag_association from being saved:</h2>

      <ul>
        <% tag_association.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="field">
    <%= form.label :task_id %>
    <%= form.text_field :task_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
